After code changes, run at least `npm run lint`; for routing or SSR changes also run `npm run build`. Next production build depends on `scripts/sync-next-server-chunks.mjs` postbuild step. Public pages that depend on optional Supabase admin/service-role access may need graceful fallbacks or `dynamic = 'force-dynamic'` to avoid build-time prerender failures when deployment envs are unavailable.